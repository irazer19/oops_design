class BookMyShow:
    def __init__(self, movie_manager, theatre_manager):
        self.theatre_manager = theatre_manager
        self.movie_manager = movie_manager

    def create_booking(self, show, seats):
        # Create all the Seats, screen, theatre, etc.
        # Now create a booking object by passing whatever the user has selected.
        # Now update the booked_seats list in the shows if the payment was successfuly.
        pass
